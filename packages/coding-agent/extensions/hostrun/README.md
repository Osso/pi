# Hostrun

First-party Pi extension package for `hostrun_eval`.

This initial scaffold registers the tool and keeps JavaScript `ctx` state alive
per Hostrun session. Host-effect helpers and the standalone MCP server are still
tracked in `docs/specs/hostrun.md`.
